import { TimelineYearsTickDirective } from './timeline-years-tick.directive';

describe('TimelineYearsTickDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineYearsTickDirective();
    expect(directive).toBeTruthy();
  });
});
