import { TimelineDayPartsTickDirective } from './timeline-day-parts-tick.directive';

describe('TimelineDayPartsTickDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineDayPartsTickDirective();
    expect(directive).toBeTruthy();
  });
});
