import { TimelineMonthsTickDirective } from './timeline-months-tick.directive';

describe('TimelineMonthsTickDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineMonthsTickDirective();
    expect(directive).toBeTruthy();
  });
});
