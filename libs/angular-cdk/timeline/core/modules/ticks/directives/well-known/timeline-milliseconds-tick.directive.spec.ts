import { TimelineMillisecondsTickDirective } from './timeline-milliseconds-tick.directive';

describe('TimelineMillisecondsTickDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineMillisecondsTickDirective();
    expect(directive).toBeTruthy();
  });
});
