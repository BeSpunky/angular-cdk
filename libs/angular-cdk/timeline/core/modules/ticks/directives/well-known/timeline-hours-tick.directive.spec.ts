import { TimelineHoursTickDirective } from './timeline-hours-tick.directive';

describe('TimelineHoursTickDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineHoursTickDirective();
    expect(directive).toBeTruthy();
  });
});
