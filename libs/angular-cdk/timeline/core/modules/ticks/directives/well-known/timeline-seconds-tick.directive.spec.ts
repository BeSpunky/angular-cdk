import { TimelineSecondsTickDirective } from './timeline-seconds-tick.directive';

describe('TimelineSecondsTickDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineSecondsTickDirective();
    expect(directive).toBeTruthy();
  });
});
