import { TimelineMinutesTickDirective } from './timeline-minutes-tick.directive';

describe('TimelineMinutesTickDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineMinutesTickDirective();
    expect(directive).toBeTruthy();
  });
});
